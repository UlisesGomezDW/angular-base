export interface Info {
    name: string
}
export type Option = {
    label: string
    value: string
}

export type Data = {
    accept: boolean
    birthDate: string
    city: string
    cologne: string
    country: string
    email: string
    firstName: string
    gender: string
    lastName: string
    name: string
    occupation: string
    postalCode: string
    state: string
    street: string
}
